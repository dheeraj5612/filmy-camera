# Agent implementation notes

## Persistent product and validation preference

- User instruction (2026-09-14): Filmy Camera will primarily be used on a vertical iPhone. Treat portrait iPhone as the primary UI/UX acceptance layout. Always test camera/control layout changes on an iPhone simulator or physical iPhone; iPhone compatibility mode on iPad is an acceptable fallback when available. Native iPad testing supplements, rather than satisfies, this requirement. Verify the viewfinder, shutter, look picker, Pro controls, dismissal, and large text in portrait before merge/release, and retain screenshots and test evidence for the current revision.

## 2026-09-13 CI review

- The catalog and UI workflows have no Swift package dependency or `Package.resolved`; Swift package download caching was therefore not added. DerivedData sharing remains unsafe across the macOS runner matrix.
- Classified `RendererRedCreaseRegressionTests` and `SignatureCharacterTests` as `integration`: both use deterministic in-memory Core Image fixtures and renderer assertions.
- Classified `RendererSkinCaptureDiagnosticsTests` as `fixtures`: its diagnostic export requires caller-supplied local capture metadata and output paths, so it remains opt-in and is skipped without the private fixture.
- Validation: `actionlint .github/workflows/catalog-acceptance.yml .github/workflows/ui-experience.yml`, inventory, and the full portable suite (`66` tests) passed.

## Build 26 / Signal Frame integration

- Integrated main `04228ed` and UX `75d9e64`, retaining renderer v19, live recipe controls and selfie mirroring. Recovery now rebuilds invalid graphs after interruption; debug capture diagnostics use complete file protection. Simulator protection attributes differ from hardware, so only that assertion is device-only.
- At `eb70442`, local Xcode 26.6/iOS 26.5 passed 468 core tests; GitHub release SDK compilation and 149 catalog acceptance tests passed. Full UI/Photos evidence remains required before merge.
- GitHub Photos fixture job `103814343176` passed the mandatory 160-photo test but rejected the new local diagnostic skip. Added its exact selector to the existing optional fixture allowlist, retaining strict required PhotoKit execution and regression coverage.

- Combined UI validation caught an undersized quick All filter (42.7pt); quick filters now guarantee 44pt width. Favorites persistence and largest Dynamic Type checks pass on iPhone 17 Pro/iOS 26.5. Updated legacy onboarding and simulator expectations for Signal Frame, and wait for stable controls before animated drawer/navigation taps. Two targeted simulator/navigation tests pass; full final CI remains the merge gate.
- iPad portrait compatibility windows scale controls to 75% in landscape. Use 64pt compact targets and element-relative test coordinates; app-normalized coordinates derived from accessibility frames can miss. Enlarged drawer/library controls retain bounded scrolling. Large-text review groups Look and Finish together; delete confirmation uses an alert with explicit Cancel. Updated simulator build passes; Photos and iPad acceptance reruns are pending.
- At `6211dbb`, iPhone acceptance passed 14/14 and local Photos passed 6/7, including split, save/relaunch, delete cancellation and permission recovery. Remaining accessibility fixes group Look/comparison beside Finish, cap library previews at 260pt, disable decorative onboarding and unavailable-preview hit testing, and put Roll's permission CTA before expanded explanatory text. A fresh-install XXXL test exposed the latter independently of previously granted Photos access. Exact final CI and Photos acceptance remain required; see PR #97 for final run evidence.
- At `75fc0e5`, actual XXXL review video showed the two-column Look title collapsing and controls below pinned actions. Use full-width accessible control rows and put actions in the same portrait content scroll. The AX test reveals and checks complete control bounds in `review-content-scroll`; normal-size controls retain their no-scroll assertions. Independent source review found no blockers; focused execution and final CI remain gates.
- iPad library evidence at `75fc0e5` showed the aspect-ratio child exceeding a 260pt maximum-height proposal. Use an explicit 220pt accessibility preview height so the first favorite action stays visible. Final integrated simulator build passes; acceptance evidence is tracked on PR #97.

- At `12965e7`, hosted iPad acceptance passed 14/14; iOS fallback hierarchy proved the native preview remained exposed despite SwiftUI hit-testing modifiers. Keep the renderer mounted, expose its diagnostic accessibility node only while running, and create live gesture/accessibility controls only for a running camera. Local Xcode 26.6/iOS 26.5 build and focused fallback, portrait rotation, and capture-setting persistence tests passed 3/3 with these changes. Hosted exact-head acceptance remains the merge gate.

- Physical iPad Pro 11-inch (2nd generation), iPadOS 26.6.2 / Xcode 26.6, baseline `3d1428e`: initial acceptance completed 38 passed, 11 failed, 1 opt-in flow skipped. Pro Done transformed to 33.5pt after rotation; match existing iPad 64pt target pattern while retaining 48pt on iPhone. Correct stale tap-focus expectations to one-shot autoFocus, reveal editor/Form controls before interaction, parse localized gallery pixel dimensions, and dismiss look drawer before gallery capture. Signed build-for-testing passes; focused physical rerun and protected PR CI remain required. Local evidence: `.ci/ipad-testflight-20260914/`, `build/release-build26-ipad/`.

- Halide-inspired control revision on dirty `435de23`: official Lux iPad screenshots informed a mirrored edge rail and contextual Exposure/Focus/White Balance sections; Filmy looks and sensor behavior remain. Enable native iPad orientations in both plist and app delegate; phone stays portrait. Rotation assertions must now distinguish idioms. Signed device build and release project preflight pass. Three physical control cases finalized successfully (3 passed, 0 failed); landscape and mirrored-grip screenshots verified. A truncated White balance selector title was corrected to wrap, with a fresh signed build passing; expanded device acceptance is running; iPhone logged 48 unit passes and two UI failures requiring follow-up. Xcode can spend ten minutes collecting diagnostics after tests; do not mistake an unfinished xcresult without Info.plist for corruption or treat test log passes as runner completion. Evidence: `.ci/ipad-testflight-20260914/halide-*` and `build/release-build26-ipad/halide-controls-tests/`.

- Additional control research: Apple iOS 26 emphasizes a clear preview (https://www.apple.com/newsroom/2025/06/apple-elevates-the-iphone-experience-with-ios-26/). Anecdotal Reddit feedback objects to covered mode labels and undiscoverable swipes (https://www.reddit.com/r/ios/comments/1t98ucm/ios_camera_mode_switcher_worst_apple_ui_ever/) and extra modal steps (https://www.reddit.com/r/iPhoneography/comments/1pv2dm3/old_ios_camera_was_better/). Retain direct labeled controls alongside gestures, contextual Pro sections, and the reachable mirrored iPad rail; do not treat these threads as representative user research.

- Hardware shutter follow-up on `879dc91` plus current-state fix: physical callback diagnostics observed an ended event with stale `scene-inactive` while the rendered view was ready. Dispatch ended events through a SwiftUI state token, then invoke the current guarded capture action in `onChange` in both Debug and Release. Do not parse debug phase strings or restrict functional routing to UI tests. Xcode26.6 signed build passed23.105s; iPadOS26.6.2 both-volume-button capture/autosave test passed1/1, zero skips (runner27.034s, source digest `bdf58e40bb5a84e09cc90fab59dec975981eabd4afad67b77aec010c3283ea43`). Evidence `.ci/ipad-testflight-20260914/volume-current-state-*`, `build/release-build26-ipad/volume-current-state-test`. Portrait iPhone CI and release checks still required.

- CI at `1b4d99c` (required run `34881723180`, Xcode 26.3) rejected implicit diagnostic property access inside the weak-self capture Task closure. Both capture/render timing assignments now use explicit `self`; production behavior is unchanged. Local signed build-for-testing passed (10.164s), evidence `.ci/ipad-testflight-20260914/explicit-self-build/run-o2b9j0ln/`. Fresh CI and portrait screenshot acceptance remain required.

## App Store Connect draft — 2026-09-14

- Live Edge check: App Store 1.0 Ready for Distribution (build 15). Public TestFlight group Filmy Camera Beta: build 22 Testing, public link https://testflight.apple.com/join/eZPPASqH; latest uploaded build 26 Ready to Submit for external testing.
- Created 1.0.1 Prepare for Submission; saved promotional text, description, release notes, and review instructions for committed Signal Frame interface c29688f. Verified persisted fields and disabled Save. Retained live name “Filmy Camera: Film Filters”, subtitle “Retro Photos & Manual Controls”, categories, URLs, and Data Not Collected privacy. Draft https://appstoreconnect.apple.com/apps/6801404866/distribution/ios/version/inflight . No build attached or review submission made. docs/app-store/metadata-en-US.md remains older copy.
- Pending: fresh iPhone/iPad screenshots and a 1.0.1 binary containing coral/black AppIcon. Uploaded build 26 predates identity; inherited screenshots are build 12. Isolated checkout /tmp/filmy-store-20260914 at c29688f. store-media used destination AF7E2AC8-A6F3-410C-84DC-A9650FBF356C, derived data /tmp/filmy-store-20260914-dd, output /tmp/filmy-store-20260914/evidence/iphone-media. Xcode 26.6 hung during preparation; stopped own xcodebuild PID 40114 to serialize with active device acceptance task 01a0a1c9-a242-7e20-958d-2380c9d48481. Exit 241, no screenshots or passing build evidence: /private/tmp/filmy-store-20260914/evidence/iphone/run-jkynh0qu/result.json . Other task owns dirty camera/service/settings changes and investigates build hang; await completion signal before retrying screenshot build.

## Updated iPad compatibility — 2026-09-14

- Connected iPad reports iPadOS27.0 (24A437); Xcode26.6 has SDK26.5. Added hardware-gated lens-smudge advisory and AirPods remote setup guidance over c29688f. Full current app Swift typecheck passed (exit0,43.545s), source digest and exact command in docs/ipados27-compatibility-20260914.md; evidence .ci/ipados27-20260914/full-typecheck/run-mql8kgno/.
- Runtime and visual acceptance remain blocked: SWBBuildService hangs at the clang version/macros probe before compilation, including serial and clean-environment retries. Direct probe succeeds. Stopped only this task’s builds and released the build slot to the store submission task. No device/UI pass or release approval inferred. Keep full-screen iPad mode until scene/window orientation, short-window controls and narrow drawer sizing are addressed. Next: resolve toolchain, run mandatory portrait iPhone and updated physical-iPad acceptance, then supported AirPods/smudge hardware checks.

## iPhone Roll thumbnail and default controls — 2026-09-14

- In CameraScreen, constrain RollThumbnail to44x44 before clipping its aspect-fill image; the previous outer frame allowed photo overflow into the Roll label. Camera controls now initialize expanded in normal launches; the explicit collapsed UI-preview fixture remains available. User can still hide controls.
- Full-app Swift6 typecheck passed,47.623s: .ci/iphone-roll-20260914/typecheck/run-vgvxtytv/result.json. App Swift path/content digest3def754c7e2591aa1ccc490b9336c529a109974ed56fbbe4d15658265c9886ee (supersedes earlier typecheck scope). git diff --check passed. Portrait simulator acceptance retry timed out before tests started (90s,exit124): .ci/iphone-roll-20260914/portrait-ui/run-u_wr07ua/result.json. Visual acceptance remains pending; no screenshot pass claimed.

## Tap-focus exposure drag — 2026-09-14

- Over c29688f plus preceding dirty compatibility/layout changes, CameraScreen now locks drag axis, applies vertical exposure from a stable starting bias after focus, shows a sun/EV badge, and resets feedback expiry after interaction. Manual mode, pinch and inactive camera block it. Added VoiceOver exposure actions. Existing horizontal look swipe remains. Research/remaining native-camera gaps: docs/native-camera-parity-20260914.md. YouTube descriptions reviewed; playback inaccessible, not claimed watched.
- Full-app command remains the direct swiftc typecheck documented above, using the existing device SwiftFileList: exit0,28.163s, .ci/focus-exposure-20260914/typecheck/run-wz9bwxem/result.json. Host policy harness compiles actual CameraPreviewGesturePolicy.swift and copied two new test bodies with assertion adapters and catalog-only stubs: compile exit0 (.ci/focus-exposure-20260914/policy-compile/run-dvsvdffj/), execution exit0 (.ci/focus-exposure-20260914/policy-run/run-ffud3lu8/), 10 assertions. Command: xcrun swiftc FilmyCamera/Models/CameraPreviewGesturePolicy.swift .ci/focus-exposure-20260914/policy/main.swift -o .ci/focus-exposure-20260914/policy/tests; then run that executable. This is policy validation, not the iOS XCTest/UI suite.
- Initial host XCTest attempts lacked Swift XCTest overlay; switched to assertion adapters. Harness exposed ambiguous CGFloat.infinity literals in new tests, corrected explicitly. No app source changed after passing typecheck. git diff --check passes. Existing Xcode build-service blocker remains: mandatory portrait iPhone visual and physical iPadOS27 gesture/capture acceptance pending, no install performed. Verify long/curved drags, edge focus points, reversal at sensor bounds, pinch, AE/AF lock, manual mode and VoiceOver on device before release.

## Pro Controls Adjust labels — 2026-09-14

- ManualCameraControlsView uses smaller caption titles and stacks icons above labels on compact iPhone widths, recovering the width previously reserved for an icon plus 24pt checkmark space. Titles retain two-line wrapping/scaling; accessibility sizes retain the single-column larger-title layout. Prior dirty changes preserved.
- Full-app direct Swift6 typecheck with the same device SDK/file-list command passed (exit0,41.39s): .ci/pro-labels-20260914/typecheck/run-fc50q916/result.json. git diff --check passed. Portrait visual acceptance remains pending under the previously documented Xcode build-service hang; no install or screenshot verification claimed.

## Xcode recovery and iOS 27 device checks — 2026-09-14

- On c29688f plus shared dirty compatibility/UI changes, fresh Xcode26.6 build-for-testing passed after host restart using build/xcode-recovery (53.089s); original build-service hang no longer reproduces, root cause unproven. Evidence: .ci/xcode-recovery/build/run-ok4uoqct/result.json. Simulator iPhone17Pro/iOS26.5 focused policy, Pro and largest-text UI tests passed11/11: .ci/xcode-recovery/iphone-tests/run-4n6z4fp6/result.json.
- Signed iPad8,9/iPadOS27.0 build succeeded. Initial automation startup timed out; one retry ran lifecycle and rotation/grip tests successfully, but Pro ISO scrolling failed at helper line1445; diagnosis pending. Evidence: .ci/xcode-recovery/ipad-retry/run-8pvot79u/result.json and ipad-retry.xcresult. This is not full device acceptance.
- Physical iPhone16Pro/iOS27.0 Pro Controls passed after unlocking (50.709s): .ci/xcode-recovery/physical-iphone-retry/run-lqpc67pj/result.json. Command: xcodebuild -project FilmyCamera.xcodeproj -scheme FilmyCamera -destination 'platform=iOS,id=00008140-00066D291A84801C' -derivedDataPath build/xcode-recovery-device -resultBundlePath .ci/xcode-recovery/physical-iphone-retry.xcresult -only-testing:FilmyCameraUITests/FilmyCameraUITests/testProControlsAdaptAndReturnToCamera test-without-building. Portrait screenshot visually confirms Exposure, Focus and White balance titles fit: .ci/xcode-recovery/physical-iphone-attachments/0FAE946E-E2FB-41DA-94C9-8F266F2E37D0.png. No hardware exposure-drag/capture acceptance claimed.
- iPad failure recording extracted at90s (.ci/xcode-recovery/ipad-pro-90.png) shows a visible ISO slider near the sheet bottom. Exported AX snapshots lack child nodes following kAXErrorServerNotFound, so hittability/enabled/frame values are unobserved. A slider containment exemption is not justified: final assertion fails exists&&isHittable. Next focused rerun should record control/scroll frames, enabled/hittable and screenshot before/after scrolling; no app layout change made on speculative cause.

## Independent Xcode build verification — 2026-09-14

- Reviewed other FilmyCamera task logs and recovery evidence. The prior SWBBuildService compiler-probe hang cleared after host restart; exact root cause is unproven. Earlier recovery build and 11 focused iPhone tests passed.
- Independently verified c29688f plus shared dirty files using Xcode 26.6 (17F113): `xcodebuild -project FilmyCamera.xcodeproj -scheme FilmyCamera -destination 'generic/platform=iOS Simulator' -derivedDataPath build/build-verification-20260914 CODE_SIGNING_ALLOWED=NO build-for-testing`. Fresh arm64/x86_64 build passed (exit 0, 214.711s). A concurrent UI-test edit prompted another build, which also passed (exit 0, 89.763s). Evidence: `.ci/build-verification-20260914/run-_ue8nriw/result.json` and `.ci/build-verification-20260914-final/run-lk0iiiz8/result.json`. No additional source fix needed for the old build hang. These runs verify compilation, not new runtime/device/release acceptance. The active iOS update task owns the separate iPad Pro Controls test follow-up.

## Roll update metadata — 2026-09-14
- Release source 754a6b2 includes local frame favorites, film/digital filters, newest/oldest/look-name sort and matching pager order. Release task reports 10 focused tests and SDK27 archive PASS.
- Edge App Store Connect draft 1.0.1 description and release notes updated for Roll features and smoother look drawer; saved then reload-readback verified, Save disabled. Local docs/app-store/metadata-en-US.md mirrors copy. No submission.
- Final release held for separately owned EXIF feature and fresh screenshots; current archive is not final until that freeze.

## Foreground stability and photo metadata — 2026-09-14
- Over root c29688f plus shared dirty changes, keep zoom row mounted through session stops/restarts; hide/disable while unavailable. Preserve source EXIF/TIFF/imported GPS, normalize output dimensions/orientation/sRGB, snapshot authorized recent location at shutter and write GPS plus Photos asset location. Main-actor location provider clears on stop/revocation; location permission description added to project.yml. Native proprietary MakerApple data remains ImageIO best effort.
- Xcode26.6/iPhone17Pro iOS26.5 simulator: 39 metadata tests, 1 physical-only skip, 0 failures, plus large-text/pro-controls UI passes in `.ci/foreground-layout/focused-final/run-4jjjor6r`. That combined run failed an initial lifecycle fixture expecting unsupported simulator flash; corrected setup. Lifecycle two-cycle control-frame checks and all-eight EXIF orientation render test then PASS (exit0), `.ci/foreground-layout/lifecycle-final/run-c_tsl3ht/result.json`. Exact commands in output logs/result bundles. Final UI fixture conditionally expands tools for physical default; physical runtime/location-dialog acceptance remains release task responsibility.
- Narrow release patch `.ci/foreground-layout/foreground-exif-release.patch` applies cleanly to release source c839128, excludes unrelated G7 changes. Regenerate Info.plist from project.yml. Xcode slot released to release task 01a0a1ca-79c2-7f60-9d4b-63ea20cf54a9 for final SDK27/device/archive/screenshots/submission.

- EXIF metadata copy follow-up: release owner reported 39 tests passed, 1 physical-only skip. Added available camera/lens/exposure/capture-time preservation, imported location retention and optional permission-based capture location; no MakerNotes guarantee. Description and release notes (including steadier camera return layout) saved and reload-verified in ASC draft1.0.1, Save disabled, Prepare for Submission. Root metadata document mirrors copy. Final archive/screenshots/submission remain release-owner gates.
- Integration correction: original combined patch is superseded. Packaging against the newer release baseline accidentally removed the Roll favorite/filter/sort helpers and recipe-selection guard. Coordinator restored both; corrected release HEAD `024c07e77037ff4057ac3eaa429408eec5804ce8`. Root reviewed app-code removals versus c839128; no other unrelated removals found, independent full-diff review pending. Do not reuse the original combined patch or regenerate with `/tmp/package_filmy_patch.py`; derive future patch from corrected release commits.
- Independent final reviewer completed full integrated `024c07e` vs `c839128` review: no concrete release blockers; Roll APIs/selection guard restored, metadata/deferred-capture plumbing and stable zoom mounting coherent. Coordinator notified to resume final archive. Real-device authorization callbacks/GPS capture and device-specific MakerApple metadata remain physical verification scope; stale/future validity policy passed unit tests.

## Final build upload — 2026-09-14
- After user signed into Xcode27, Organizer confirmed final 1.0.1(27) upload complete at 8:35 PM. Exact archive build/release-1.0.1-27-submit/FilmyCamera.xcarchive (release-owner validated e4969e8, SDK27).
- App Store Connect TestFlight readback: build27 Processing. No review submission. Release task owns processing completion, build selection, fresh screenshot gates and final submission.

- Build27 processing completed (Ready to Submit). Selected 27/version1.0.1 in ASC draft1.0.1; save/reload confirmed with Save disabled. Binary App Icon preview visually matches release Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png coral/black nested F; docs/app-store/icon/AppIconMaster.png is stale cream/gold reference, not current binary icon. No review submission; fresh screenshot gate still owned by release task.

- Published optional location/EXIF policy: isolated legal repo `.ci/legal-location-20260914`, commit `9e65cd1d5f04e3ab5e32c50cac87870e32303c5d`; targeted policy text covers When In Use geotags, available imported metadata, no developer uploads, Photos sync/share choices, revocation and existing saved tags. HTML tag validation and diff check passed. Pages run `34914516485` passed via gh run watch exit0; live HTTP200 bytes match source SHA256 `ea747ba1849d774770a53e48be6a2d66327d6f53f6f671be7ac49e08a17e0469`. Evidence `.ci/legal-location-evidence/live-verification.json`, `.ci/legal-location-evidence/run-lcfq4lxg/result.json`. Release coordinator notified; no app-source or release-tree changes.

## App Review submission completed — 2026-09-14
- Final 1.0.1(27), archive source e4969e8 SDK27, submitted at 9:09 PM. Reloaded ASC confirms Waiting for Review. Submission ID 1af65677-697c-4f7a-b61a-f32be7394b11.
- Both fresh final-source iPhone6.5 and iPad13 five-image sets uploaded and order01–05 verified after reload; updated Roll/EXIF/layout metadata and automatic release after approval retained.
- URL: https://appstoreconnect.apple.com/apps/6801404866/distribution/reviewsubmissions/details/1af65677-697c-4f7a-b61a-f32be7394b11

- Release coordinator confirmed final 1.0.1 (27), source `e4969e8`, archive/export/upload/processing passed. Fresh five iPhone and five iPad screenshots visually reviewed and validated; iPad run `.ci/release-27-final-media/ipad-command/run-ouc9_ymx/result.json` exit0, media validation `.ci/release-27-final-media/validation/run-qjl4d7_1/result.json` exit0 (root read both). App Store submission `1af65677-697c-4f7a-b61a-f32be7394b11` reload-verified Waiting for Review Sept14 2026 9:09PM EDT; automatic release retained. Device GPS/permission-callback verification not performed; simulator policy/encoding checks passed. App not publicly released until Apple approval.

## TestFlight and personal device install — 2026-09-14
- TestFlight group Filmy Camera Beta now includes 1.0.1(27), submitted to Beta App Review and reload-verified Waiting for Review. Public link https://testflight.apple.com/join/eZPPASqH enabled; 0 testers and no pending approvals displayed. Build22 remains Testing until27 approved.
- Exported exact final submitted archive build/release-1.0.1-27-submit/FilmyCamera.xcarchive with debugging/automatic signing, no rebuild. Export PASS: .ci/device-install-27/export-log/run-iz3u4ph6/result.json. IPA Info.plist verified1.0.1(27)/com.dheeraj.filmycamera, codesign --verify --deep --strict PASS; provision includes both connected devices.
- devicectl install app PASS on iPhone16Pro and iPadPro11-inch2ndgen without uninstall. Evidence .ci/device-install-27/iphone-install/run-zp4l11do/result.json and .ci/device-install-27/ipad-install/run-lybozylh/result.json. devicectl process launch com.dheeraj.filmycamera exited0 and confirmed launch on each. No additional visual/device GPS acceptance claimed.
